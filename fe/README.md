# Flight Radar Frontend

Requires Node.js 24+ and npm.

Start the WebSocket backend.

In a second terminal, start the frontend:

```bash
npm i && npm run dev
```

Open the app: http://localhost:5173.

Ensure `fe/.env` contains the backend URL.
